const path = require('path');

module.exports = {
    config: path.resolve(__dirname, 'app', 'config', 'database.ts'),
    'migrations-path': path.resolve(__dirname, 'app', 'database', 'migrations'),
};